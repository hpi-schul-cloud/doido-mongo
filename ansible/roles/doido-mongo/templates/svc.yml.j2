apiVersion: v1
kind: Service
metadata:
  name: doido-mongo-svc
  namespace: {{ NAMESPACE }}
  labels:
    app: doido-mongo
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: doido-mongo